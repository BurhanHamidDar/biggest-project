Plan:
1. Update `transportController.js` to enforce phone number for drivers.
2. Verify `StudentDetailScreen.tsx` displays Roll No (it does).
3. Notify user that backend now rejects requests without mobile/roll no.
